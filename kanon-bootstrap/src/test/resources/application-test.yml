spring:
  datasource:
    url: jdbc:tc:postgresql:16:///datalithix_kanon?TC_REUSABLE=true
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    mongodb:
      host: ${embedded.mongo.host}
      port: ${embedded.mongo.port}
  security:
    enabled: false
